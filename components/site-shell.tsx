'use client';
import { useEffect,useState } from 'react';
import {usePathname} from 'next/navigation';
import {ArrowUpRight,Waves,Menu} from 'lucide-react';
import {Sheet,SheetTrigger,SheetContent,SheetHeader,SheetTitle,SheetDescription} from '@/components/ui/sheet';
const links=[['/','หน้าแรก'],['/#model','กระบี่โมเดล'],['/#policies','4 นโยบาย'],['/#roadmap','แผนขับเคลื่อน'],['/team','กลุ่ม GP6'],['/documents','เอกสาร']];
export function Header(){const [open,setOpen]=useState(false);const [active,setActive]=useState('');const pathname=usePathname();useEffect(()=>{if(pathname!=='/')return;const obs=new IntersectionObserver(es=>{es.forEach(e=>{if(e.isIntersecting)setActive(e.target.id)})},{rootMargin:'-15% 0px -60% 0px'});['home','model','policies','roadmap'].forEach(id=>{const el=document.getElementById(id);if(el)obs.observe(el)});return()=>obs.disconnect()},[pathname]);const isCurrent=(href:string)=>href===pathname&&pathname!=='/'||pathname==='/'&&(href===`/#${active}`||href==='/'&&active==='home');return <><a className="skip-link" href="#main">ข้ามไปเนื้อหา</a><header className="site-nav"><a className="brand" href="/" aria-label="GP6 Krabi Model หน้าแรก"><Waves/><strong>GP6<span>KRABI MODEL</span></strong></a><nav aria-label="เมนูหลัก">{links.map(([href,label])=><a key={href} href={href} aria-current={isCurrent(href)?'location':undefined}>{label}{href==='/documents'&&<ArrowUpRight size={15}/>}</a>)}</nav><div className="mobile-menu"><Sheet open={open} onOpenChange={setOpen}><SheetTrigger aria-label="เปิดเมนู"><Menu/></SheetTrigger><SheetContent><SheetHeader><SheetTitle>GP6 | KRABI MODEL</SheetTitle><SheetDescription>สำรวจผลงานกลุ่ม GP6 รุ่นที่ 104</SheetDescription></SheetHeader><nav className="mobile-links" aria-label="เมนูมือถือ">{links.map(([href,label])=><a key={href} href={href} onClick={()=>setOpen(false)}>{label}<ArrowUpRight size={18}/></a>)}</nav></SheetContent></Sheet></div></header></>}
export function Footer(){return <footer><div className="container footer-top"><a className="brand" href="/"><Waves/><strong>GP6<span>KRABI MODEL</span></strong></a><p>สร้างคุณค่าจากทะเล พัฒนาคน<br/>กระจายประโยชน์สู่ชุมชน</p><a href="/documents" className="text-link">อ่านผลงานกลุ่ม <ArrowUpRight size={17}/></a></div><div className="container footer-bottom"><span>ผลงานการศึกษา GP6 • นบส.1 รุ่นที่ 104 • ประจำปี 2569</span><span>รายงาน: ลิขสิทธิ์ของสำนักงาน ก.พ.</span></div></footer>}
